events {
}

http {


    server {
        listen 443 ssl;
        server_name mcbetools.trashdev.org;

        # SSL certificates
        ssl_certificate /etc/letsencrypt/live/trashdev.org/fullchain.pem;
        ssl_certificate_key /etc/letsencrypt/live/trashdev.org/privkey.pem;

        # SSL protocols and session settings
        ssl_protocols TLSv1.2 TLSv1.3;
        ssl_session_cache shared:SSL:10m;
        ssl_session_timeout 10m;

        client_max_body_size 40M;

        location / {
            proxy_pass http://localhost:3000;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;
proxy_no_cache 1;
proxy_cache_bypass 1;
        }
  
        location /api/ {
            rewrite ^/api/(.*) /$1 break;
            proxy_pass http://localhost:3001;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;
proxy_no_cache 1;
proxy_cache_bypass 1;
        }
    }
    server {
    server_name mcbetools.com www.mcbetools.com;

    root /var/www/mcbetools-cilent;
    index index.html;

    location / {
            proxy_pass http://localhost:3000;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;
        }
    location /api/ {
            rewrite ^/api/(.*) /$1 break;
            proxy_pass http://localhost:3001;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;
        }

    listen 443 ssl; # managed by Certbot
    ssl_certificate /etc/letsencrypt/live/mcbetools.com/fullchain.pem; # managed by Certbot
    ssl_certificate_key /etc/letsencrypt/live/mcbetools.com/privkey.pem; # managed by Certbot
    include /etc/letsencrypt/options-ssl-nginx.conf; # managed by Certbot
    ssl_dhparam /etc/letsencrypt/ssl-dhparams.pem; # managed by Certbot


}


    server {
        listen 443 ssl;
        server_name code.trashdev.org;

        # SSL certificates
        ssl_certificate /etc/letsencrypt/live/trashdev.org/fullchain.pem;
        ssl_certificate_key /etc/letsencrypt/live/trashdev.org/privkey.pem;

        # SSL protocols and session settings
        ssl_protocols TLSv1.2 TLSv1.3;
        ssl_session_cache shared:SSL:10m;
        ssl_session_timeout 10m;

        client_max_body_size 40M;
        root /var/www/html;
        index index.html;

        location / {
            proxy_pass http://localhost:5771;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;
        }
    }


    server {
        listen 443 ssl;
        server_name openzschat.trashdev.org;

        # SSL certificates
        ssl_certificate /etc/letsencrypt/live/trashdev.org/fullchain.pem;
        ssl_certificate_key /etc/letsencrypt/live/trashdev.org/privkey.pem;

        # SSL protocols and session settings
        ssl_protocols TLSv1.2 TLSv1.3;
        ssl_session_cache shared:SSL:10m;
        ssl_session_timeout 10m;

        client_max_body_size 40M;

        location / {
            proxy_pass http://localhost:5021;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;
        }
    }

    server {
        listen 443 ssl;
        server_name api.trashdev.org;

        # SSL certificates
        ssl_certificate /etc/letsencrypt/live/trashdev.org/fullchain.pem;
        ssl_certificate_key /etc/letsencrypt/live/trashdev.org/privkey.pem;

        # SSL protocols and session settings
        ssl_protocols TLSv1.2 TLSv1.3;
        ssl_session_cache shared:SSL:10m;
        ssl_session_timeout 10m;

        client_max_body_size 40M;

        location /api/ {
            rewrite ^/api/(.*) /$1 break;
            proxy_pass http://localhost:3001;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;
        }
    }

    server {
        listen 80;
        server_name mcbetools.trashdev.org;
        return 301 https://mcbetools.trashdev.org$request_uri;
    }

    server {
    if ($host = www.mcbetools.com) {
        return 301 https://$host$request_uri;
    } # managed by Certbot


    if ($host = mcbetools.com) {
        return 301 https://$host$request_uri;
    } # managed by Certbot


    listen 80;
    server_name mcbetools.com www.mcbetools.com;
    return 404; # managed by Certbot




}}

